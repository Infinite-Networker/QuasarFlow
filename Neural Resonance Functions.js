neural_resonance_function calculateNeuralNetwork(input) {
    // Neural network calculations that resonate with your brain
    let result = input * 2;  // Bug fix: 'result' was used but never defined; now properly initialized
    return result;
}

let input = 21;  // Bug fix: 'input' was referenced but never declared before the function call
let output = calculateNeuralNetwork(input);
